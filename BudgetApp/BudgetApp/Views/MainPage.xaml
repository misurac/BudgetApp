<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BudgetApp.MainPage" BackgroundColor="Beige">

    <StackLayout>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Label Text="Monthly Budget" FontAttributes="Bold" FontSize="18" />
        <Entry Grid.Column="1" Placeholder="" />
        <Label Grid.Row="1" Text="Remaining Budget :" FontAttributes="Bold" FontSize="18"/>
        <Entry Grid.Row="1" Grid.Column="1" Placeholder="" />
        <Button x:Name="AddNewExpense" Text="Add Expense" Clicked="AddNewExpense_Clicked" Margin="40,20,40,20" Font="Bold" FontSize="20"
                BackgroundColor="LightGray" />
        <Grid BackgroundColor="LightYellow" WidthRequest="-5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Width="30*" />
                <ColumnDefinition Width="30*" />
                <ColumnDefinition Width="30*" />
                <ColumnDefinition Width="30*" />
            </Grid.ColumnDefinitions>
            <Label  Grid.Column="0" Text="Name" />
            <Label  Grid.Column="1" Text="Date"/>
            <Label Grid.Column="2" Text="Amount"/>
            <Label Grid.Column="3" Text="Category"/>
        </Grid>
        <ListView x:Name="ExpenseRecords" ItemsSource="{Binding expenses}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>

                                <ColumnDefinition  Width="30*" />
                                <ColumnDefinition Width="30*" />
                                <ColumnDefinition Width="30*" />
                                <ColumnDefinition Width="30*" />

                            </Grid.ColumnDefinitions>

                            <Label  Grid.Column="0" Text="{Binding ExpenseName}" FontAttributes="Bold"/>
                            <Label  Grid.Column="1" Text="{Binding Date}"/>
                            <Label Grid.Column="2" Text="{Binding Amount}"/>
                            <Label Grid.Column="3" Text="{Binding Category}"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
