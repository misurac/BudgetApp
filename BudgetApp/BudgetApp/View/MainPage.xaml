<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BudgetApp.View.MainPage"
             Title="MainPage">
    <StackLayout>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Label Text="Monthly Budget" />
        <Entry Grid.Column="1" Placeholder="Enter your Monthly Budget" />
        <Label Grid.Row="1" Text="Remaining Budget" />
        <Entry Grid.Row="1" Grid.Column="1" Placeholder="Calculate the Remaining Budget" />

        <Button x:Name="AddNewExpense" Text="AddNewExpense" Clicked="AddNewExpense_Clicked"/>
        <ListView x:Name="ExpenseRecords"
                  Margin="20"
                  ItemSelected="ExpenseRecord_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>